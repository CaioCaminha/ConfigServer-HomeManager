server:
   port: 8888
   
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CaioCaminha/microservice-repo
          search-paths: regionais
#A branch onde o arquivo está inserido deve ser master